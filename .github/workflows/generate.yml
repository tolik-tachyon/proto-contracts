name: Generate Protobuf

on:
  push:
    branches: ["main"]

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Protoc
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Install plugins
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

      - name: Add Go bin to PATH
        run: echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Generate
        run: |
          rm -rf out
          mkdir -p out

          protoc \
            --go_out=out \
            --go-grpc_out=out \
            payment/payment.proto

      - name: Checkout generated repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/tolik-tachyon/proto-generated.git

      - name: Sync files
        run: |
          rm -rf proto-generated/paymentpb
          mkdir -p proto-generated/paymentpb
          cp -r out/github.com/tolik-tachyon/proto-generated/paymentpb/* proto-generated/paymentpb/

      - name: Commit & push
        run: |
          cd proto-generated
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "auto generate proto" || echo "no changes"
          git push